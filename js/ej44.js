let texto = "HOLA";
let primero = texto.charAt(0);
let segudo = texto.charAt(1);
let tercero = texto.charAt(2);
let cuarto = texto.charAt(3);
let resultado = cuarto + tercero + segudo + primero;
//el resto de los caracteres no se muestra por son los espacios
console.log(resultado.toLowerCase());